<Window x:Class="TarkovTracker.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="SayserTarkovTracker Overlay"
        Width="800"
        Height="800"
        MinWidth="300"
        MinHeight="300"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        Topmost="True"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="True">

    <Border x:Name="OverlayRootBorder"
            Background="#331E1E1E"
            BorderBrush="#777777"
            BorderThickness="1"
            CornerRadius="6">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="38"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0"
                       Background="#AA2D2D30"
                       LastChildFill="True"
                       MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">

                <Button Content="X"
                        Width="32"
                        Height="24"
                        DockPanel.Dock="Right"
                        Margin="0,6,6,6"
                        Click="CloseButton_Click"/>

                <StackPanel DockPanel.Dock="Right"
                            Orientation="Horizontal"
                            VerticalAlignment="Center"
                            Margin="0,0,8,0">
                    <TextBlock Text="Opacity"
                               Foreground="White"
                               VerticalAlignment="Center"
                               Margin="0,0,6,0"/>

                    <Slider x:Name="OpacitySlider"
                            Width="130"
                            Minimum="20"
                            Maximum="100"
                            Value="80"
                            TickFrequency="5"
                            IsSnapToTickEnabled="False"
                            VerticalAlignment="Center"
                            ValueChanged="OpacitySlider_ValueChanged"/>
                </StackPanel>

                <TextBlock Text="SayserTarkovTracker Overlay"
                           Foreground="White"
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           Margin="10,0,0,0"/>
            </DockPanel>

            <Grid Grid.Row="1"
                  Background="Transparent">
                <wv2:WebView2 x:Name="OverlayMapView"
                              DefaultBackgroundColor="Transparent"/>
            </Grid>

            <ResizeGrip Grid.RowSpan="2"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Bottom"
                        Width="18"
                        Height="18"
                        Margin="0,0,3,3"/>
        </Grid>
    </Border>
</Window>
